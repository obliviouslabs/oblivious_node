name: unit

on:
  pull_request:
  push:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  sanity:
    name: Code sanity checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy, llvm-tools-preview
      - name: Install codespell
        run: python -m pip install --upgrade pip && python -m pip install codespell
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -A missing-docs
      - name: Run codespell
        run: codespell --quiet-level=2 --skip="backup,target,.git" -L inout

  test:
    name: Run tests
    needs: [sanity]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - uses: taiki-e/install-action@nextest
      - name: Install reth test binary
        run: |
          set -euo pipefail
          image="ghcr.io/paradigmxyz/reth@sha256:505fca5e87d6411fc7117f29a87e42787ec2eebe8225506e9ddb4759ae2ba5cc"
          mkdir -p /tmp/reth-bin
          cid="$(docker create --entrypoint /bin/sh "$image" -c true)"
          trap 'docker rm -f "$cid" >/dev/null 2>&1 || true' EXIT
          if docker cp "$cid:/usr/local/bin/reth" /tmp/reth-bin/reth 2>/dev/null; then
            :
          else
            docker cp "$cid:/usr/bin/reth" /tmp/reth-bin/reth
          fi
          chmod +x /tmp/reth-bin/reth
          /tmp/reth-bin/reth --version
      - name: Run tests (collect JUnit)
        run: cargo nextest run --workspace --no-fail-fast --profile ci
      - name: Upload test results to Codecov (optional)
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: target/nextest/ci/junit.xml

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - uses: taiki-e/install-action@nextest
      - uses: taiki-e/install-action@cargo-llvm-cov
      - name: Install reth test binary
        run: |
          set -euo pipefail
          image="ghcr.io/paradigmxyz/reth@sha256:505fca5e87d6411fc7117f29a87e42787ec2eebe8225506e9ddb4759ae2ba5cc"
          mkdir -p /tmp/reth-bin
          cid="$(docker create --entrypoint /bin/sh "$image" -c true)"
          trap 'docker rm -f "$cid" >/dev/null 2>&1 || true' EXIT
          if docker cp "$cid:/usr/local/bin/reth" /tmp/reth-bin/reth 2>/dev/null; then
            :
          else
            docker cp "$cid:/usr/bin/reth" /tmp/reth-bin/reth
          fi
          chmod +x /tmp/reth-bin/reth
          /tmp/reth-bin/reth --version
      - name: Run coverage (llvm-cov)
        run: cargo llvm-cov --lcov --output-path target/lcov.info nextest --workspace --no-fail-fast
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: target/lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}

  unit-success:
    name: unit success
    runs-on: ubuntu-latest
    needs: [test, coverage]
    if: always()
    steps:
      - name: Decide whether the needed jobs succeeded or failed
        uses: re-actors/alls-green@release/v1
        with:
          jobs: ${{ toJSON(needs) }}
